[project]
name = "rolodex"
version = "0.1.0"
description = "Interview Intelligence System"
requires-python = ">=3.10"

[project.scripts]
rolodex = "backend.main:cli"

[tool.setuptools.packages.find]
where = ["."]
